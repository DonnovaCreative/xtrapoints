---
// Customer Support page (/support). Footer-linked. Content is CMS-managed via the
// `supportPage` singleton in Sanity (Legal & Compliance → Customer Support). The
// rendered body lives in SupportPageView.astro (shared with the on-demand draft
// preview, src/pages/preview/support.astro); this file just supplies published
// data.
import SupportPageView from "@/components/SupportPageView.astro";
import { getSupportPage } from "@/data/supportSource";

const page = (await getSupportPage()) ?? {
  title: "Customer Support",
  intro: "",
  email: "",
  hours: "",
  address: "",
  body: [],
};
---

<SupportPageView page={page} />
